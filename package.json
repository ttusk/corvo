{
  "name": "corvo",
  "version": "0.1.0",
  "private": true,
  "description": "Corvo is an Obsidian plugin for managing study cycles for public exams.",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "esbuild": "^0.25.5",
    "jsdom": "^29.1.1",
    "obsidian": "^1.8.10",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
